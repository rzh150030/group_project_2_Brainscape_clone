const GET_DECKS = 'decks/SET_DECKS';
const REMOVE_DECK = 'decks/REMOVE_DECK';



const initialState = { };

export default function reducer(state = initialState, action) {
  switch (action.type) {

    default:
      return state;
  }
}
